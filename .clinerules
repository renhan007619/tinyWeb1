# 个人学习档案

## 基本信息
- **学校**：重庆交通大学
- **专业**：交通运输（非计算机专业，双非本科）
- **年级**：大一下（开学第十一 周）
- **学习方式**：自学为主

## 职业规划
- **目标**：跨专业进入计算机行业
- **目标企业**：大厂优先，其次中大厂
- **技术栈**：Go语言（云原生方向，大厂需求多，可区分竞争力）
- **发展方向**：后端开发 + Agent开发（根据行业趋势灵活调整）
- **实习规划**：
  - 大二暑假：开始找实习（小公司也可接受）
  - 后续：跳槽大厂实习 → 转正或校招进大厂
- **最终目标**：通过技术面试，获得 offer

## 技能基础
- **编程语言**：C语言（已学完）、Go（从tinyweb项目学习）
- **数据结构与算法**：数据结构课程已学完，LeetCode 70题（后期会补刷）
- **操作系统**：学到线程概念（课程视频1/3），正在进行哈工大 hit-oslab（已完成lab2）
- **项目经验**：
  - tinyweb项目：正在解构理解，力争每行代码都理解，目前进行到第11天任务
  - 使用AI Agent协助开发，学习项目架构和逻辑
- **技术输出**：
  - 稀土掘金博客：130篇左右（记录学习过程、笔记、思考）
  - GitHub：持续贡献，目标一年满屏绿色

## 当前学习项目
- **主要项目1**：哈工大操作系统实验（hit-oslab）
  - 进度：正在学习实验2（操作系统的引导），进程调度尚未开始
  - 目标：深入理解操作系统原理
  
- **主要项目2**：tinyweb项目解构
  - 进度：第11天任务
  - 方法：逐行理解代码逻辑
  - 原则：当前阶段吃透tinyweb即可，更有竞争力的项目计划大二上准备

## 学习偏好与要求
- **核心原则**：理解深刻，注重学习效果
- **知识重点**：
  - 贴近求职目标，面试常考知识要详细讲解
  - 底层知识积累（培养计算机思维）
  - 对职业发展有帮助的知识重点讲
  
- **问答风格**：
  - 详细讲解原理，突出重点
  - 结合面试场景讲解    
  - 底层思维培养

---

## 后续学习计划
- **计算机网络**：最晚大一下暑假开始
- **数据库**：最晚大一下暑假开始

## 待补充信息（可选）
- [ ] 操作系统实验遇到的具体难点
- [ ] 希望重点提升的技能方向
- [ ] 需要额外关注的计算机基础课程

## 服务器部署信息

### 服务器连接
- **服务器地址**：`ssh user@1.15.224.88`

### 上传服务器流程
1. **本地Git操作**：
   - `git add .`
   - `git commit -m "提交信息"`
   - `git push origin <分支名>`

2. **登录服务器**：
   - `ssh user@1.15.224.88`
   - 查看服务器项目结构：`ls -la`
   - 确认项目运行目录和子目录结构

3. **服务器端拉取代码**：
   - `cd <项目目录>`
   - `git pull origin <分支名>`

4. **⚠️ 关键：静态文件环境配置（最容易出错）**
   - 项目使用 `STATIC_DIR` 环境变量指定静态文件目录
   - **必须设置**：`export STATIC_DIR=/path/to/fronted`
   - 路径指向 `fronted` 目录（包含 index.html 的目录）
   - 根据 `config/config.go` 代码：
     - `StaticDir` 默认值为空，程序会使用当前工作目录
     - 服务器上必须显式设置 `STATIC_DIR` 指向正确的 fronted 目录
   - **验证方法**：启动后检查日志中的 `📂 静态文件:` 路径是否正确

5. **重启服务**：
   - 停止旧进程（如有）：`pkill -f "server.exe"` 或 `pkill -f "main"`
   - 重新编译（如需）：`go build -o server.exe main.go`
   - 后台运行：`nohup ./server.exe > server.log 2>&1 &`

### 环境变量配置示例
```bash
# 数据库配置（根据服务器实际情况设置）
export DB_HOST=localhost
export DB_PORT=3306
export DB_USER=root
export DB_PASS=your_password
export DB_NAME=tinyweb1

# ⚠️ 关键：静态文件目录必须正确指向 fronted
export STATIC_DIR=/home/user/tinyweb1/fronted

# 服务器端口
export SERVER_PORT=8080

# 运行环境
export APP_ENV=production
```

## LeetCode刷题策略（面试官培养模式）

**角色定位**：大厂面试官视角，从初学者循序渐进培养至资深工程师。通过算法训练建立计算机底层思维，为技术面试和职业发展奠定基础。

### 时间安排
- **当前阶段（至暑假）**：每天1题，晚上完成tinyweb/oslab后执行；题量稳定后再调整时段
- **底线原则**：项目任务优先，算法题不可挤占项目深度理解时间
- **题单来源**：使用代码随想录题单，自主安排

### 核心要求

**1. 精刷 + 体系建构**
- 每题深度解构：数据结构选择逻辑、算法步骤拆解、边界条件分析
- 建立思维模板：同类题目总结"解题框架"（如双指针适用场景）
- 拒绝题海战术：30道精刷题 > 100道走马观花

**2. AI辅助策略**
- **第一步独立**：15分钟独立思考，手写伪代码（禁止看AI）
- **第二步对比**：完成后对比AI代码，分析差异（时间/空间效率对比）
- **第三步追问**：问AI"为什么这样优化？"、"还有哪些边界情况？"
- **工具思维**：AI是"资深面试官"，用来检验和扩展思路，非答案生成器

**3. 复杂度深度掌握**
- **必答项**：时间复杂度 + 空间复杂度 + 优化可能性
- **底层理解**：为什么是O(n)不是O(n²)？空间换时间的具体代价？
- **文档记录**：每题必须记录复杂度分析到笔记/博客

**4. 项目联动（核心）**
- **联动触发器**：刷题遇到的数据结构/算法，主动联想tinyweb/oslab中的实际应用
- **反向操作**：做项目时遇到复杂逻辑，去LeetCode找相关题目
- **具体映射**：
  - 进程调度 → 任务调度器相关题目
  - 路由匹配 → Trie树相关题目
  - 内存管理 → 链表/堆相关题目

**5. 好奇心驱动（面试预埋）**
- **多问一个为什么**：不仅满足于"能做出来"
- **扩展问题示例**：
  - 如果数据规模扩大10倍怎么办？
  - 如果内存受限怎么办？
  - 这道题的bug通常出现在哪？
- **面试价值**：深度问题展示"抗压能力"和"思维深度"

### 必遵循的四步刷题法
```
① 独立思考15分钟 → ② 看题解理解思路 → ③ 独立编码 → ④ 隔天默写复习
```
- 缺少任何一步，学习效果减半
- 第④步是关键，基于"间隔重复"原理

### 补充机制

**双链笔记**
- 必含：题目描述、我的思路vs最优解差异、复杂度分析、项目关联点、面试追问

**难度控制**
- **当前**：Easy为主（建立自信），Medium为辅（挑战思维）
- **避开**：Hard题（性价比低，面试极少考）
- **节奏**：每周6 Easy + 1 Medium，适应后调整

**复盘机制**
- **每周日**：回顾本周7题，总结"解题模式"
- **每月底**：统计最薄弱的数据结构类型，针对性补题

---

## 备注
- 学习计划自行制定，不需要推荐路线
- 服务器部署时特别注意 `STATIC_DIR` 环境变量的配置

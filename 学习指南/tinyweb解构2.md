# tinyWeb1 深度学习任务清单（解构2）— 大厂面试官拷打版

> **目标**：深度理解项目每一行代码，积累工程思维，能应对真实场景（安全、性能、扩展），**能扛住大厂面试深挖**。
> **适用阶段**：已完成 Day 1-11，理解代码含义后，进入串联与深度阶段。
> **使用方式**：不要按天推进，按模块自选。每个任务必须有"理解输出"——能说清楚原理、能定位代码、能解释取舍。

---

## 【模块A】MySQL 深度实操（从GORM到SQL本质）

**目标**：不只是"看懂代码"，而是能在MySQL里手写SQL实现同样功能，理解性能 implications

### A1. 登录服务器，用命令行操作真实数据库
- `ssh user@1.15.224.88`
- `mysql -u root -p tinyweb1`
- `SHOW TABLES`、`DESCRIBE visit_stats` 对照 model.go 的tag
- **理解输出**：为什么 `visitor_ip` 是 `varchar(45)` 而不是 `varchar(15)`？（提示：IPv6！）

### A2. 手写SQL还原GORM操作
- 写出 `RecordVisit` 里 `First` → `Create/Updates` 对应的完整SQL
- 写出 `GetVisitStats` 里三条 `Select` 对应的SQL
- 写出 `focus.go` 里 `GROUP BY` 查询的SQL
- **理解输出**：GORM的 `First` 为什么生成 `LIMIT 1`？不用 `LIMIT 1` 会怎样？

### A3. 索引分析与性能预判
- `SHOW INDEX FROM visit_stats`、`SHOW INDEX FROM users`
- `EXPLAIN SELECT * FROM visit_stats WHERE visitor_ip='xxx'`
- **理解输出**：如果 `visitor_ip` 没索引，100万条记录查询会怎样？现在项目为什么还没崩？

### A4. 连接池参数实验
- 当前 `MaxOpenConns=10`，在 db.go 里改成1和100分别测试
- 观察并发请求时的表现（用浏览器多标签同时刷新）
- **理解输出**：连接池满了以后新请求会怎样？阻塞还是报错？实际观察到的现象是什么？

### A5. 【面试拷打】InnoDB索引原理
- 查资料：InnoDB为什么用B+树而不是B树或哈希？
- 查资料：聚簇索引 vs 非聚簇索引的区别
- **理解输出**：`visit_stats` 表的`id`字段和`visitor_ip`索引，分别是什么类型的索引？数据存储方式有什么不同？

---

## 【模块B】剩余Handler自读验证（检验模式识别能力）

**目标**：不看文档，自己读代码，验证"CRUD模式"是否真正内化

### B1. todo.go 自读 + 笔记
- 通读全文，标注和 `visit.go` 的相同点（代码结构）和不同点（业务逻辑）
- **理解输出**：`todo.go` 的权限检查在哪里？为什么 `visit.go` 没有？

### B2. guestbook.go 自读 + 笔记
- 同样方法读一遍
- **理解输出**：留言板有没有做内容审核？如果有人刷垃圾留言怎么办？

### B3. setting.go + page.go + admin_visitors.go 快速过
- 重点关注 `admin_visitors.go` 的权限控制
- **理解输出**：管理员接口和普通接口的区别是什么？怎么防止普通用户访问 admin 接口？

### B4. 对比总结：6个handler的"代码模板"
- 抽象出一个"标准CRUD handler"的模板结构
- **理解输出**：如果新增一个"文章管理"功能，最少需要改哪几个文件？每处改动的理由？

### B5. 【面试拷打】表设计缺陷分析
- 当前 `visit_stats` 用IP做唯一键，有什么问题？
- `focus` 模块的tag是字符串存储，如果tag数量爆炸怎么优化？
- **理解输出**：如果让你重新设计表结构，你会改哪三个地方？为什么？

---

## 【模块C】安全防护实战（小红书推广必备）

**目标**：发现真实漏洞，理解攻击原理，亲手修复

### C1. SQL注入攻击测试
- 尝试在留言板提交 `' OR '1'='1`
- 查看 `guestbook.go` 的查询代码，确认是否安全
- **理解输出**：GORM的参数化查询是怎么防注入的？如果换成字符串拼接会怎样？（尝试写出不安全的代码）

### C2. XSS漏洞挖掘与修复
- 在留言板提交 `<script>alert(document.cookie)</script>`
- 观察是否执行，查看前端渲染代码
- 对比 `AuthManager` 里的 `escapeHtml` 函数
- **理解输出**：为什么 `textContent` 安全而 `innerHTML` 危险？项目中哪些地方用了 `innerHTML`？逐个定位行号

### C3. JWT安全分析
- 查看 `utils/jwt.go`，确认密钥存储方式
- 查看浏览器DevTools → Application → LocalStorage 里的token
- 尝试在 jwt.io 解码token，能看到什么？
- **理解输出**：密钥硬编码有什么风险？如果泄露了密钥，攻击者能做什么？（尝试自己生成一个假token）

### C4. 接口限流设计与实现
- 当前 `/api/visit` 和 `/api/auth/register` 没有限流
- 思考：如果有人写脚本狂刷这两个接口，会有什么后果？
- 手写一个简单的"IP限流中间件"（用map记录IP访问次数，超过阈值返回429）
- **理解输出**：限流应该放在哪一层？middleware还是handler？为什么？

### C5. 密码安全深度理解
- 查看 `utils/hash.go`，确认bcrypt的cost参数
- 理解：为什么同一密码每次哈希结果不同？
- **理解输出**：如果数据库泄露，攻击者拿到bcrypt哈希，能反推密码吗？为什么？（查资料：彩虹表攻击、bcrypt设计原理）

### C6. 【面试拷打】CSRF与Token刷新
- 当前有没有CSRF防护？如果用户在A网站点击链接，触发了你网站的删除操作？
- JWT 24h过期，但没有刷新机制，用户用着用着被踢出去怎么办？
- **理解输出**：设计一个"滑动过期"的token刷新方案，怎么实现？

### C7. 【面试拷打】上传安全
- 查看 `page.go` 的上传功能，有什么安全隐患？
- 如果有人上传WebShell（伪装成图片的PHP/Go代码），怎么防御？
- **理解输出**：文件上传应该做哪些校验（类型、大小、内容）？分别怎么实现？

---

## 【模块D】高并发与稳定性（小红书推广后的现实问题）

**目标**：理解项目瓶颈，预判崩溃点，学会基础监控

### D1. 数据库瓶颈分析
- `RecordVisit` 的 `SELECT → UPDATE` 逻辑，高并发下会有什么问题？
- 查资料：什么是"竞态条件"？什么是"乐观锁"？
- **理解输出**：如果两个请求同时查到 `visit_count=5`，都+1变成6，而不是7，怎么解决？（提示：数据库层面、应用层面各有什么方案？）

### D2. 内存Session的局限
- 查看 `session/memory.go`，确认存储方式
- 思考：服务器重启后，已登录用户会怎样？
- **理解输出**：为什么生产环境用Redis而不是内存？除了持久化还有什么好处？

### D3. 日志系统搭建
- 当前项目有日志吗？日志记在哪里？
- 给关键handler加上 `log.Printf`，记录请求IP、URL、耗时
- **理解输出**：如果用户报告"登录不了"，你打算怎么从日志定位问题？（模拟一次故障排查）

### D4. 简单的性能测试
- 用浏览器同时开10个标签狂刷页面，观察服务器反应
- 登录服务器，运行 `htop` 或 `top` 命令，观察CPU和内存占用
- **理解输出**：瓶颈在CPU、内存还是数据库？怎么判断？

### D5. 【面试拷打】容量规划与QPS估算
- 当前 `MaxOpenConns=10`，假设每个请求处理100ms，理论QPS是多少？
- 如果小红书上热门，瞬间1万UV涌入，哪里会先崩？
- **理解输出**：设计一个"流量增长10倍"的扩展方案，需要改哪些地方？

---

## 【模块E】功能改动实战（检验理解深度）

**目标**：真正动手改，体会"牵一发而动全身"**

**选一个做，做完必须能解释：改了哪些文件？每处为什么这样改？**

### E1. 简单：访问统计加"城市"字段
- 需求：根据IP显示访问者所在城市
- 涉及：model.go（加字段）、visit.go（IP解析）、index.html（显示）
- **理解输出**：IP解析应该在前端还是后端做？为什么？

### E2. 中等：留言板加"回复"功能
- 需求：支持嵌套回复，形成树形结构
- 涉及：model.go（加parent_id）、guestbook.go（递归查询）、前端（树形渲染）
- **理解输出**：数据库怎么设计才能支持无限层级？查询时会不会有N+1问题？怎么解决？

### E3. 挑战：专注时间加"中断记录"
- 需求：记录每次专注被打断的原因和时间点
- 涉及：新表设计、focus.go新增接口、前端UI改动
- **理解输出**：一对多关系在数据库和Go代码里分别怎么表示？

### E4. 【面试拷打】技术方案选型
- 城市IP解析：用本地库（如GeoIP）还是调用第三方API？利弊？
- 树形回复：数据库用邻接表还是嵌套集？各适合什么场景？
- **理解输出**：如果数据量达到100万条，你的方案还适用吗？需要改什么？

---

## 【模块F】设计取舍与工程思维（面试储备）

**目标**：理解"为什么这样设计"，培养技术决策能力

### F1. JWT vs Session 深度对比
- 当前项目同时用了JWT和内存Session，分别用在哪？
- 思考：能不能只用JWT？能不能只用Session？
- **理解输出**：什么场景适合JWT？什么场景适合Session？为什么两者都用？

### F2. 前后端分离 vs 不分离
- 当前项目前端在 `fronted/`，后端在 `server/`，是分离还是不分离？
- 思考：为什么 `index.html` 是一个4000+行的大文件？
- **理解输出**：如果重构，你会把前端拆成Vue/React组件吗？利弊各是什么？

### F3. 错误处理机制分析
- 查看所有handler的错误处理模式，`sendJSON` 的错误码设计
- 思考：前端怎么根据不同的错误码给用户不同的提示？
- **理解输出**：`code: 0` 表示成功是行业惯例吗？如果设计错误码体系，你会怎么设计？

### F4. 配置管理方案
- 查看 `config/config.go`，环境变量怎么读取？默认值怎么设？
- 思考：数据库密码明文写在环境变量里安全吗？还有什么方案？
- **理解输出**：生产环境的敏感配置应该怎么管理？（查资料：K8s Secret、Vault、阿里云KMS等）

### F5. 【面试拷打】技术栈选型复盘
- 为什么选择Go+MySQL+内存Session？如果重来你会怎么选？
- 当前是单体架构，如果拆微服务，你会按什么维度拆？
- **理解输出**：微服务拆分的原则是什么？你的项目适合拆吗？为什么？

### F6. 【面试拷打】团队协作与代码质量
- 前端4000+行单文件，如果团队5个人协作，怎么保证代码不冲突？
- 项目有单元测试吗？覆盖率多少？怎么保证重构不出问题？
- **理解输出**：设计一个代码规范检查流程（如pre-commit hook），需要检查什么？

---

## 【模块G】工程思维提炼（可迁移能力）

**目标**：把"这个项目"的经验抽象成"下一项目也能用"的能力

### G1. 面试问答准备
- 用STAR法则准备：在这个项目里你解决了什么问题？
  - Situation：背景是什么？
  - Task：你的任务是什么？
  - Action：你做了什么？（具体到代码层面）
  - Result：结果如何？（数据、稳定性、安全性）
- 准备3个"亮点"：比如"我发现并修复了XSS漏洞"、"我优化了查询性能"

### G2. 技术博客写作
- 选一个有深度的点写博客（如：从零实现JWT认证、SQL注入防御实战）
- 目标：让读的人能跟着复现
- **理解输出**：写作过程中，你发现哪些知识点自己其实没懂透？

### G3. 建立个人代码库
- 把常用的模式抽出来：
  - JWT中间件模板
  - CRUD handler模板
  - 限流中间件模板
  - 配置读取模板
- **理解输出**：下次新项目，哪些代码可以直接复制？哪些必须重新设计？

---

## 【模块H】架构推演与压力测试（面试高频）

**目标**：具备容量规划意识，能回答"流量涨10倍怎么办"

### H1. 容量估算与QPS计算
- 当前配置（MaxOpenConns=10，假设每次查询100ms）理论QPS是多少？
- 统计真实项目的API响应时间（在日志里加时间戳）
- **理解输出**：如果连接池打满，新的请求会排队还是报错？Go的HTTP server怎么处理积压请求？

### H2. 压力测试实战
- 安装 `wrk` 或 `ab` 工具
- 对 `/api/visit/stats` 和 `/api/auth/login` 分别压测
- 记录QPS、延迟P99、错误率
- **理解输出**：压测过程中CPU、内存、MySQL连接数的变化曲线？瓶颈最先出现在哪？

### H3. 垂直扩容 vs 水平扩容
- 单服务器升级到16核64G内存，能解决所有问题吗？
- 多实例部署会遇到什么问题？（session共享、数据库连接数翻倍）
- **理解输出**：你的项目更适合垂直扩容还是水平扩容？为什么？

### H4. 【面试拷打】多实例架构设计
- 画一个支持3个实例的部署架构图
- session共享怎么解决？（Redis？粘性会话？）
- 负载均衡怎么做？（Nginx？云厂商SLB？）
- **理解输出**：如果其中一个实例挂了，负载均衡怎么知道？健康检查怎么做？

---

## 【模块I】容灾与高可用（生产必备）

**目标**：理解"服务不崩"比"功能正确"更难

### I1. MySQL主从复制与读写分离
- 查资料：MySQL主从复制原理（binlog、relay log）
- 当前单MySQL实例，如果挂了怎么办？
- **理解输出**：设计一个读写分离方案，哪些读操作可以走从库？写操作怎么处理延迟？

### I2. Redis替换内存Session
- 部署Redis（单机或Docker）
- 改写 `session/memory.go` 为 `session/redis.go`
- 验证多实例下session共享
- **理解输出**：Redis和内存相比，除了持久化还有什么优势？（数据结构丰富、原子操作、过期策略）

### I3. 健康检查与故障转移
- 设计 `/api/health` 接口，检查数据库连接、Redis连接
- 查资料：Kubernetes的livenessProbe和readinessProbe区别
- **理解输出**：健康检查应该检查什么？太轻无法发现问题，太重消耗资源，怎么平衡？

### I4. 数据库备份与恢复
- 编写备份脚本（`mysqldump`或物理备份）
- 模拟数据误删，执行恢复
- **理解输出**：全量备份+增量备份 vs 实时主从，各适合什么场景？RTO和RPO是什么意思？

### I5. 【面试拷打】分布式锁与竞态条件
- 如果多实例部署，`RecordVisit` 的 `SELECT→UPDATE` 怎么保证原子性？
- 查资料：分布式锁的实现方式（Redis setnx、MySQL唯一索引、ZooKeeper）
- **理解输出**：用Redis实现一个分布式锁，需要处理哪些边界情况？（锁过期、锁续期、可重入）

---

## 【模块J】代码重构与工程化（资深开发）

**目标**：代码从"能跑"到"可维护"

### J1. 统一错误码体系
- 设计错误码规范（如 0=成功，1000-1999=参数错误，2000-2999=业务错误，5000+=系统错误）
- 改写所有handler的错误返回
- 前端根据错误码显示不同提示
- **理解输出**：错误码设计应该考虑哪些因素？（可读性、扩展性、国际化）

### J2. 日志框架升级
- 引入 `zap` 或 `logrus` 替换标准库log
- 实现分级日志（debug/info/warn/error）
- 添加日志轮转（按时间或大小切割）
- 关键路径添加TraceID，便于请求追踪
- **理解输出**：为什么生产环境需要结构化日志（JSON格式）？ELK stack是做什么的？

### J3. 单元测试与接口测试
- 为 `utils/hash.go` 和 `utils/jwt.go` 写单元测试
- 为 `handler/visit.go` 写接口测试（用 `httptest` 包）
- 计算测试覆盖率（`go test -cover`）
- **理解输出**：TDD（测试驱动开发）适合你现在的项目吗？为什么？

### J4. 配置中心与热更新
- 引入 `viper` 库管理配置
- 支持从文件、环境变量、远程配置中心读取
- 实现配置热更新（不重启服务生效）
- **理解输出**：配置中心（如Apollo、Nacos）解决了什么问题？你的项目需要吗？

### J5. 【面试拷打】数据库迁移方案
- 当前 `AutoMigrate` 有什么问题？（删字段不会删列，改类型可能丢数据）
- 查资料：Flyway、Liquibase、GORM Migration的区别
- 设计一个零停机数据库迁移方案（如加字段用online DDL）
- **理解输出**：如果迁移过程中出问题，怎么回滚？数据一致性怎么保证？

---

## 【模块K】底层原理深挖（技术深度）

**目标**：不止于"会用"，要懂"为什么这样设计"

### K1. GORM源码阅读
- clone GORM仓库，找到 `First` 方法的实现
- 追踪：First → Take → query → rows → Scan 的调用链
- 理解预处理语句（Prepared Statement）是怎么工作的
- **理解输出**：GORM为什么要缓存stmt？性能提升多少？什么情况下不缓存？

### K2. bcrypt算法原理
- 查资料：bcrypt的cost因子、盐值生成、密钥派生过程
- 对比：bcrypt vs scrypt vs Argon2，各适合什么场景？
- **理解输出**：为什么bcrypt能抵抗GPU/ASIC暴力破解？内存硬（memory-hard）算法是什么意思？

### K3. JWT与加密算法
- 查资料：HS256（HMAC-SHA256）vs RS256（RSA-SHA256）的区别
- 为什么JWT签名可以防止篡改？（数字签名原理）
- **理解输出**：如果换成RS256，密钥怎么管理？服务端和客户端各持有什么？

### K4. Go HTTP服务器原理
- 阅读 `net/http` 源码，理解 `ServeMux` 的路由匹配（Trie树？）
- 理解 `http.Server` 的连接处理流程（Accept → Handler → Response）
- 查资料：Go的goroutine调度模型（GMP模型）
- **理解输出**：为什么Go适合写高并发服务器？goroutine比OS线程轻量在哪？

### K5. MySQL事务与锁
- 查资料：ACID特性、四种隔离级别、脏读/不可重复读/幻读
- 实验：在两个session里模拟并发修改同一行，观察锁行为
- **理解输出**：`RecordVisit` 的 `SELECT→UPDATE` 在不同隔离级别下分别有什么问题？怎么解决？

### K6. 【面试拷打】操作系统与网络
- TCP三次握手、四次挥手的过程，用Wireshark抓包验证
- HTTP/1.1 vs HTTP/2 vs HTTP/3的区别，你的项目能用HTTP/2吗？
- Linux的epoll是什么？为什么比select/poll高效？
- **理解输出**：从浏览器输入URL到页面显示，经历了哪些步骤？（DNS、TCP、TLS、HTTP、渲染）

---

## 学习建议

1. **每次只做一件事**：选一个有状态的模块，做完标记完成
2. **输出检验**：每个任务后面都有"理解输出"，必须能口头或文字回答
3. **遇到卡点记下来**：这是你的知识缺口，比完成任务更重要
4. **定期回顾**：每周看一遍清单，划掉的越多理解越深
5. **模拟面试**：每完成一个【面试拷打】任务，对着镜子或AI复述答案

---

## 优先级建议（大厂面试准备）

**P0 - 面试前必须完成**（架构、安全、性能核心）：
- F1-F2 架构设计、F5 技术选型复盘
- C1-C5 安全基础、C6-C7 安全进阶
- D1 数据库竞态条件、D5 容量规划
- H2-H4 压测与多实例架构

**P1 - 加分项**（工程化、高可用）：
- I2 Redis替换session、I5 分布式锁
- J1-J2 错误码与日志、J5 数据库迁移
- K4-K5 Go原理与MySQL事务

**P2 - 长期积累**（底层原理）：
- K1-K3 源码阅读、K6 操作系统网络

**P3 - 按需选择**（功能扩展）：
- E 模块功能改动、G 模块博客输出

---

## 30个大厂面试问题清单（自检用）

<details>
<summary>点击查看全部面试问题（用于自测）</summary>

### 架构设计类
1. 为什么选择Go+MySQL+内存Session？如果重来你会怎么选？
2. 当前是单体架构，如果拆微服务，你会按什么维度拆？
3. 前端4000+行单文件，如果团队5个人协作，怎么保证代码不冲突？
4. 数据库表设计里，visit_stats用IP做唯一键，有什么问题？IPv6环境下呢？
5. 专注时间模块的tag是字符串存储，如果tag数量爆炸怎么优化？

### 性能优化类
6. RecordVisit的SELECT→UPDATE，并发高时会出现什么问题？怎么解决？
7. GetVisitStats每次都要SUM全表，数据量大了怎么办？你做过压测吗？
8. 连接池MaxOpenConns=10，怎么确定这个数？QPS多少时会打满？
9. 前端fetch是串行的，能不能优化？HTTP/2了解吗？
10. 如果小红书上热门，瞬间1万UV涌入，哪里会先崩？预案是什么？

### 安全类
11. XSS漏洞具体在哪里？除了escapeHtml还有什么方案？
12. JWT存在localStorage，如果用户电脑中木马，token被盗怎么办？
13. bcrypt的cost=10，计算一次100ms，如果攻击者用10台机器并发撞库，怎么防？
14. 有没有CSRF防护？如果用户在A网站点击链接，触发了你网站的删除操作？
15. 上传功能有没有限制？如果有人上传WebShell怎么办？

### 分布式与高可用类
16. 内存Session，服务器重启用户全掉线，怎么解决？Redis集群了解吗？
17. 单MySQL实例，如果挂了怎么办？主从复制做过吗？
18. 部署是单点吗？怎么做到滚动更新不停服？
19. 如果多实例部署，session共享怎么解决？JWT还需要session吗？
20. 限流除了IP维度，用户维度怎么做？怎么防止刷注册？

### 工程实践类
21. 错误处理都是if err != nil，有没有统一的错误码设计？前端怎么区分？
22. 项目有单元测试吗？覆盖率多少？怎么保证重构不出问题？
23. 日志怎么打的？如果用户报告登录失败，你怎么定位？
24. 配置管理环境变量分散，如果参数越来越多，怎么管理？
25. 数据库迁移怎么做的？如果改表结构，线上已有数据怎么处理？

### 底层原理类
26. GORM的First方法底层怎么实现的？预处理语句了解吗？
27. bcrypt的盐值存在哪？为什么同一密码哈希结果不同？
28. JWT的签名是HS256，原理是什么？和RS256有什么区别？
29. Go的goroutine和OS线程什么关系？你的项目用了goroutine吗？
30. MySQL的InnoDB索引是B+树，为什么选B+树不是B树或哈希？

</details>

---

> **记住**：能扛住面试拷打的核心是——**不仅知道怎么做，更知道为什么这么做，以及为什么不那样做**。
